# Shapefile Generator

A lightweight Python tool to convert coordinate pairs into ESRI Shapefiles (`.shp`) with embedded Coordinate Reference System (`.prj`) metadata to prevent coordinate system errors.

## Requirements

Install dependencies using pip:

```bash
pip install -r requirements.txt


I recommend keeping STAGE20_ARCHITECTURE_REVIEW.md, but you no longer need to read it before every task. For future Stage 21+ work, the normal sequence should simply be:

AGENTS.md → ARCHITECTURE.md → relevant source/tests → work.

The Stage 20 review only needs to be opened when we want the detailed reasoning behind an architectural decision.